name: example

description: Example app for flutter_mordern_chat_ui

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_mordern_chat_ui: any

flutter:
  uses-material-design: true